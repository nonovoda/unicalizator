[phases.setup]
language = "python"
version = "3.10"
nixPkgs = [
  "zlib",
  "libjpeg",
  "freetype",
  "gcc",
  "pkg-config"
]
[phases.build]
command = "python3 -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt"
